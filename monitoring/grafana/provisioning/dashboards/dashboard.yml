apiVersion: 1

providers:
  - name: 'revoagent-dashboards'
    orgId: 1
    folder: 'reVoAgent'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards